// SPA mode: render entirely on the client (no server, no prerender). Data is
// fetched client-side via the forge, using the user's token from the auth store.
export const ssr = false;
export const prerender = false;
