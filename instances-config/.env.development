# Development overrides, loaded on top of .env by `npm run dev` and by builds
# from any branch other than main, staging or testing.

# Topic stamped on every repo created here, used to build the listing.
PUBLIC_REPO_TOPIC=lets-encode-dev
